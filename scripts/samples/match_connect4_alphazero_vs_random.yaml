# Match: AlphaZero vs Random on Connect4
# Usage: cp scripts/samples/match_connect4_alphazero_vs_random.yaml scripts/simulate_match.yaml && python scripts/simulate_match.py
# Update agent1.checkpoint_dir to point to your trained agent

game: connect4

match:
  num_games: 100

agent1:
  type: alphazero
  checkpoint_dir: saved_agents/YOUR_CHECKPOINT_HERE

agent2:
  type: random
